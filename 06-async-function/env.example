# ============================================
# FastAPI 異步編程實戰 - 環境變數範例
# ============================================

# ========== Redis 配置 ==========
# Redis 主機地址
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 資料庫編號（0-15）
REDIS_DB=0

# Redis 密碼（如果有的話，留空表示無密碼）
REDIS_PASSWORD=

# ========== Celery 配置 ==========
# Celery Broker URL（任務佇列）
CELERY_BROKER_URL=redis://localhost:6379/0

# Celery Backend URL（結果儲存）
CELERY_BACKEND_URL=redis://localhost:6379/0

# ========== 應用程式配置 ==========
# 應用程式環境（development, production）
ENVIRONMENT=development

# 日誌級別（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# ========== 使用說明 ==========
# 1. 複製此檔案為 .env
#    cp env.example .env
#
# 2. 根據你的環境修改配置值
#
# 3. 啟動 Redis（使用 Docker Compose）
#    docker-compose -f build/docker/docker-compose.yaml up -d
#
# 4. 啟動 Celery Worker（如果需要）
#    celery -A celery_app worker --loglevel=info
#
# 5. 啟動 FastAPI 應用
#    uvicorn main:app --reload

